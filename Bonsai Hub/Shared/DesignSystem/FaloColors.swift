//
//  FaloColors.swift
//  Bonsai World
//
//  Semantic system colors for Falo chrome (labels, quiet separators).
//  Window fills use SwiftUI `.windowBackground` / `.faloScrollSurface()` instead.
//

import SwiftUI

enum FaloColors {
    static let textSecondary = Color.secondary
    static let borderSubtle = Color.primary.opacity(0.08)
}
